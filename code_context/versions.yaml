# Optional release aliases only. Keys map product labels → git SHA or tag.
# Do not map dev/unknown/mock — those are rejected by the code context manager.
# Prefer stamping the real git SHA or tag into appliance builds.
releases:
  appliance-console: {}
  appliance-backend: {}
